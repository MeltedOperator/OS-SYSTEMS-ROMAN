# Структура лабораторной работы os-lab02

Так как некоторые вопросы объясняются в классе, я запишу их здесь, ибо
я знаю что они по крайней мере у меня есть.

# 1) Насчет pitfall
Разница с set -euo pipefail довольно очевидна: в первом случае когда мы не поставили эту команду,
даже после вывода ошибки с "не найденной директорией" код продолжил выполняться. Во втором же случае
set -euo pipefail просто не позволила коду исполняться далее. 
Я думаю это особо полезно когда в коде сотня-тысяча
строчек которые завязаны на директории и если что то пойдет не так то будет выводиться не один десяток ошибок
в консоль, что не очень хорошо, особенно когда отдельные блоки кода связаны между собой. В этом же случае ошибка
покажется только раз и ее можно будет исправить

# Тесты с fileinfo:
1)Без аргументов ./fileinfo   -   Использование: ./fileinfo.sh <файл>

2)Несуществующий файл ./fileinfo.sh /tmp/no_such_file    -   Ошибка: '/tmp/no_such_file' не найден или не является файлом

3)Неисполняемый файл ./fileinfo.sh /etc/passwd       
=== Информация о файле ===
Имя: /etc/passwd
Строк: 28
Размер: 4.0K
Исполняемый: нет

4)Исполняемый файл ./fileinfo.sh greet.sh
=== Информация о файле ===
Имя: greet.sh
Строк: 7
Размер: 4.0K
Исполняемый: да

# Тесты с logstat.sh
logstat.sh - это файл, который принимает директорию(по умолчанию это /logs) и на выходе дает количество строк в них 

1) с аргументом ./logstat.sh ~/os-lab02/logs - 
=== Статистика логов в /home/melted/os-systems-work/os-lab02/logs ===
  app_1.log                      89 строк
  app_2.log                      56 строк
  app_3.log                      78 строк
  app_4.log                      16 строк
  app_5.log                      105 строк
===========================
Итого: 344 строк в 5 файлах


2) без дефолтного значения cd ~/os-systems-work/os-lab02 && ./logstat.sh
=== Статистика логов в ./logs ===
  app_1.log                      89 строк
  app_2.log                      56 строк
  app_3.log                      78 строк
  app_4.log                      16 строк
  app_5.log                      105 строк
===========================
Итого: 344 строк в 5 файлах

3) несуществующая директория ./logstat.sh /tmp/no_such_dir
Ошибка: директория '/tmp/no_such_dir' не найдена


# Тесты с logstat_v2.sh 
logstat_v2.sh - это обновленная версия файла logstat.sh где код разделен на функции 
validate_args() который проверяет существование директории и в случае провала возвращает
ошибку, count_file_lines() которая принимает в качестве аргумента файл и считает его строчки
и main() которая проходится по всем файлам в директории и считает каждую строчку в ней, а 
после выводит общее количество строчек

Старая версия)./logstat.sh ~/os-systems-work/os-lab02/logs
=== Статистика логов в /os-lab02/logs ===
  app_1.log                      89 строк
  app_2.log                      56 строк
  app_3.log                      78 строк
  app_4.log                      16 строк
  app_5.log                      105 строк
===========================
Итого: 344 строк в 5 файлах

Новая версия) ./logstat_v2.sh ~/os-systems-work/os-lab02/logs
=== Статистика логов ===
  app_1.log                      89 строк
  app_2.log                      56 строк
  app_3.log                      78 строк
  app_4.log                      16 строк
  app_5.log                      105 строк
Итого: 344 строк 

По структуре кода новая версия чище и выполняет ровно те же самые функции. Новая версия также
не выводит из какой директории считаются строки в файлах 
